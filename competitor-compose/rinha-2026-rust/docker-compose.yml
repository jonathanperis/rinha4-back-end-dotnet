services:
  api1: &api
    image: ghcr.io/gabrielrauch/rinha-2026:ed38fb4fbace6a473863ecf3ece455c5523063e1
    environment:
      - RINHA_FD_SOCK=/run/sock/api1.sock
    volumes:
      - sock:/run/sock
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "160M"
    ulimits:
      nofile:
        soft: 65535
        hard: 65535

  api2:
    <<: *api
    environment:
      - RINHA_FD_SOCK=/run/sock/api2.sock

  lb:
    image: ghcr.io/gabrielrauch/rinha-2026-lb:ed38fb4fbace6a473863ecf3ece455c5523063e1
    restart: always
    depends_on:
      - api1
      - api2
    environment:
      - PORT=9999
      - FD_UPSTREAMS=/run/sock/api1.sock,/run/sock/api2.sock
      - WORKERS=2
    ports:
      - "9999:9999"
    volumes:
      - sock:/run/sock
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: "30M"
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
volumes:
  sock:
