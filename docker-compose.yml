name: rinha-2026-jonathanperis

services:
  webapi1: &webapi
    image: ${WEBAPI_IMAGE:-ghcr.io/jonathanperis/rinha4-back-end-dotnet:ci-d384902a41ca819eeaccdb10503a8d4db8e97bc2}
    pull_policy: always
    platform: linux/amd64
    cpuset: "0"
    user: root
    restart: always
    read_only: true
    security_opt:
      - seccomp=unconfined
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      DOTNET_SYSTEM_GLOBALIZATION_INVARIANT: "1"
      DOTNET_PROCESSOR_COUNT: "1"
      DOTNET_SYSTEM_NET_SOCKETS_INLINE_COMPLETIONS: "1"
      DOTNET_SYSTEM_NET_SOCKETS_THREAD_COUNT: "1"
      ACCEPT_LOOPS: "2"
      MIN_WORKER_THREADS: "128"
      SCORER_MODE: hybrid
      SOCKET_PATH: /sockets/api1.sock
      DATA_DIR: /data
      EXACT_PATH: /data/references.bin
      IVF_PATH: /data/references.ivf.bin
      IVF_FAST_NPROBE: "1"
      IVF_FULL_NPROBE: "1"
      IVF_BOUNDARY_FULL: "false"
      IVF_BBOX_REPAIR: "true"
      IVF_REPAIR_MIN_FRAUDS: "0"
      IVF_REPAIR_MAX_FRAUDS: "5"
      IVF_ZERO_FAST_APPROVE_WORST_DISTANCE: "5000000"
      IVF_FIVE_FAST_DENY_WORST_DISTANCE: "4000000"
      BUCKET_PATH: /data/references.bucket.bin
      BUCKET_EARLY_CANDIDATES: "9800"
      BUCKET_MIN_CANDIDATES: "16150"
      BUCKET_MAX_CANDIDATES: "24200"
      BUCKET_PROFILE_FASTPATH: "true"
      BUCKET_REFERENCE_FASTPATH: "true"
      BUCKET_REFERENCE_FASTPATH_LEGIT: "false"
      BUCKET_REFERENCE_FASTPATH_FRAUD: "true"
      BUCKET_REFERENCE_FASTPATH2_LEGIT: "true"
      BUCKET_REFERENCE_FASTPATH2_FRAUD: "true"
      BUCKET_PROFILE_LEGIT_MIN_COUNT: "5"
      BUCKET_PROFILE_FRAUD_MIN_COUNT: "15"
      BUCKET_EXACT_FALLBACK: "risky"
      BUCKET_AVX_CUTOFF_DIMS: "6"
    tmpfs:
      - /tmp
    volumes:
      - sockets:/sockets

    deploy:
      resources:
        limits:
          cpus: "0.42"
          memory: "160MB"
    ulimits:
      nofile:
        soft: 65535
        hard: 65535

  webapi2:
    <<: *webapi
    cpuset: "1"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      DOTNET_SYSTEM_GLOBALIZATION_INVARIANT: "1"
      DOTNET_PROCESSOR_COUNT: "1"
      DOTNET_SYSTEM_NET_SOCKETS_INLINE_COMPLETIONS: "1"
      DOTNET_SYSTEM_NET_SOCKETS_THREAD_COUNT: "1"
      ACCEPT_LOOPS: "2"
      MIN_WORKER_THREADS: "128"
      SCORER_MODE: hybrid
      SOCKET_PATH: /sockets/api2.sock
      DATA_DIR: /data
      EXACT_PATH: /data/references.bin
      IVF_PATH: /data/references.ivf.bin
      IVF_FAST_NPROBE: "1"
      IVF_FULL_NPROBE: "1"
      IVF_BOUNDARY_FULL: "false"
      IVF_BBOX_REPAIR: "true"
      IVF_REPAIR_MIN_FRAUDS: "0"
      IVF_REPAIR_MAX_FRAUDS: "5"
      IVF_ZERO_FAST_APPROVE_WORST_DISTANCE: "5000000"
      IVF_FIVE_FAST_DENY_WORST_DISTANCE: "4000000"
      BUCKET_PATH: /data/references.bucket.bin
      BUCKET_EARLY_CANDIDATES: "9800"
      BUCKET_MIN_CANDIDATES: "16150"
      BUCKET_MAX_CANDIDATES: "24200"
      BUCKET_PROFILE_FASTPATH: "true"
      BUCKET_REFERENCE_FASTPATH: "true"
      BUCKET_REFERENCE_FASTPATH_LEGIT: "false"
      BUCKET_REFERENCE_FASTPATH_FRAUD: "true"
      BUCKET_REFERENCE_FASTPATH2_LEGIT: "true"
      BUCKET_REFERENCE_FASTPATH2_FRAUD: "true"
      BUCKET_PROFILE_LEGIT_MIN_COUNT: "5"
      BUCKET_PROFILE_FRAUD_MIN_COUNT: "15"
      BUCKET_EXACT_FALLBACK: "risky"
      BUCKET_AVX_CUTOFF_DIMS: "6"

  lb:
    image: jrblatt/so-no-forevis:v0.0.2
    pull_policy: always
    platform: linux/amd64
    cpuset: "2,3"
    user: root
    restart: always
    security_opt:
      - seccomp=unconfined
    environment:
      PORT: "9999"
      UPSTREAMS: /sockets/api1.sock,/sockets/api2.sock
      BUF_SIZE: "4096"
      WORKERS: "1"
    volumes:
      - sockets:/sockets
    depends_on:
      - webapi1
      - webapi2
    ports:
      - "9999:9999"
    deploy:
      resources:
        limits:
          cpus: "0.16"
          memory: "30MB"
    ulimits:
      nofile:
        soft: 65535
        hard: 65535

volumes:
  sockets:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: size=4m,mode=0777
